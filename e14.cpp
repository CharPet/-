#include <stdio.h>

int main() {
    char name[50];  // Πίνακας χαρακτήρων για αποθήκευση του ονόματος

    // Ζητάει από τον χρήστη να εισάγει το όνομά του
    printf("Πως σε λενε; ");
    scanf("%s", name);  // Διαβάζει το όνομα του χρήστη

    // Εκτυπώνει έναν φιλικό χαιρετισμό
    printf("Γεια σου, %s! Καλώς ήρθες!\n", name);

    return 0;
}
